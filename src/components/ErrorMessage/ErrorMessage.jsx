import css from "./ErrorMessage.module.css"

const ErrorMessage = () => {
  return (
    <div><p> className={css.errorMessage}Something wrong, try again...</p></div>
  )
}

export default ErrorMessage